//: Playground - noun: a place where people can play

import UIKit

var zeichen = Set<Character>()

zeichen.insert("A")
zeichen.insert("B")

var macIterator = zeichen.makeIterator()
macIterator.next()

/*
 
 var gerade : Set<Int> = [0,2,4,6,8]
 
 let geradPrim = primzahl.intersection(gerade) gerade und geradPrim sind set
 var isSubset = ungeradPrim.isSubset(of: gerade)
 
 let xor = primZahl.symmetricDifference(ungerad)
 
 let maxArray = [String](macCpu.values) -> change dict to array
  let macsArray = Array<String>(macCpu.keys)
 
 */

var zahlName = [Int: String]()


